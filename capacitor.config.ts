import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion.vv2.intento.2',
  appName: 'evaluacion-2-intento-2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
